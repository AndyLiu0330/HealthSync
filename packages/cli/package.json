{
  "name": "@healthsync/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "healthsync": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@healthsync/core": "workspace:*",
    "commander": "^12.1.0",
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
